# Benchmark results

- `fidelity.json` — conversion accuracy vs reference TFLite (real, committed).
- On-device latency / thermal JSON drops here once `bench_inference.py` runs on
  the A733. The raw `*.jsonl` sample logs are gitignored; the `*_summary.json`
  and the refreshed charts are committed.
